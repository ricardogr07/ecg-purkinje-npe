// The finding, in one sentence, above the spectrum. Copy lifted verbatim from
// the frozen standalone. No hedging, no jargon: it must land in ninety seconds.
export default function Hero() {
  return (
    <div className="max-w-4xl">
      <h1 className="text-4xl font-bold tracking-tight text-zinc-50 sm:text-6xl">
        A 12-lead ECG pins down four of seven Purkinje conduction parameters.
      </h1>
      <p className="mt-5 text-lg leading-relaxed text-zinc-300 sm:text-xl">
        Here is which ones, and here is how we know we are not fooling ourselves.
      </p>
      <p className="mt-5 max-w-2xl text-sm leading-relaxed text-zinc-500">
        Amortized neural posterior estimation over seven conduction parameters at fixed anatomy,
        graded with simulation based calibration on the simulator. Synthetic target, no patient
        data.
      </p>
    </div>
  );
}
